{
  "className": {
    "prefix": "cn",
    "body": ["className={styles.${1:}}"]
  },
  "superEnum": {
    "prefix": "en",
    "body": [
      "export const ${1:} = {",
      "  ELEMENT_1: 1,",
      "} as const",
      "",
      "export type ${1} = keyof typeof ${1}",
      "export type ${1}Code = (typeof ${1})[${1}]",
      "export const ${1}ByCode = Object.fromEntries(",
      "  Object.entries(${1}).map(([key, value]) => [value, key]),",
      ") as Record<${1}Code, ${1}>",
      "",
      "export const ${1}Ru: Record<${1}, string> = {",
      "  ELEMENT_1: 'элемент 1',",
      "}"
    ]
  }
}
